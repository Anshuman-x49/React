import { Outlet } from 'react-router'

const Dashboardlayout = () => {
    return (
        <div>
            <h1>navbar</h1>
            <Outlet />
        </div>
    )
}

export default Dashboardlayout